description: List of resource local IDs or unique identifiers (URIs).
oneOf:
  - type: array
    title: Local IDs 
    minItems: 1
    items:
      type: string
      minLength: 1
  - type: array
    title: Unique IDs
    minItems: 1
    items:
      type: string
      format: uri