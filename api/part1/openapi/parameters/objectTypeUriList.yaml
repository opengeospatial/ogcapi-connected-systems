
name: objectType
description: |-
  List of object type URIs.
  Only properties that are of one of the specified object types are selected.
in: query
required: false
schema:
  type: array
  minItems: 1
  items:
    type: string
    format: uri
explode: false