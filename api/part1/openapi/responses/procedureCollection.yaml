description: OK. Successful request
content:
  application/json:
    schema:
      type: object
      required: items
      properties:
        items:
          type: array
          items:
            $ref: ../schemas/geojson/procedure.json
        links:
          type: array
          items:
            $ref: ../schemas/common/commonDefs.json#/definitions/Link
  application/geo+json:
    schema:
      type: object
      required: features
      properties:
        type:
          type: string
          const: FeatureCollection
        features:
          type: array
          items:
            $ref: ../schemas/geojson/procedure.json
        links:
          type: array
          items:
            $ref: ../schemas/common/commonDefs.json#/definitions/Link
  application/sml+json:
    schema:
      type: object
      required: items
      properties:
        items:
          type: array
          items:
            $ref: ../schemas/sensorml/procedure.json
        links:
          type: array
          items:
            $ref: ../schemas/common/commonDefs.json#/definitions/Link