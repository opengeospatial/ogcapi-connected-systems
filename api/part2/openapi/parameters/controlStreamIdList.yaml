
name: control
description: |-
  List of control stream local IDs or unique IDs (URI).
  Only commands that are part of a control stream that has one of the provided identifiers are selected.
in: query
required: false
schema:
  $ref: idListSchema.yaml
explode: false