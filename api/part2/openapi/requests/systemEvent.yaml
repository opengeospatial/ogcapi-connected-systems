description: A single `System Event` resource
content:
  application/om+json:
    schema:
      $ref: ../schemas/json/systemEvent.json
      properties:
        links:
          description: Links to related resources
          $ref: ../schemas/common/commonDefs.json#/definitions/Links