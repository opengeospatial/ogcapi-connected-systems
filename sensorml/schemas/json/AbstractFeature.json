{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "type": {
      "type": "string"
    },
    "id": {
      "description": "Locally unique ID of the object (e.g. unique to a server or to a particular dataset).",
      "type": "string"
    },
    "description": {
      "description": "A textual description of the object.",
      "type": "string"
    },
    "uniqueId": {
      "description": "URI serving as a globally unique identifier for the object, typically a URN",
      "type": "string",
      "format": "uri"
    },
    "name": {
      "description": "A human readable label for the object.",
      "type": "string"
    }
  },
  "required": [
    "type", "uniqueId", "name"
  ]
}